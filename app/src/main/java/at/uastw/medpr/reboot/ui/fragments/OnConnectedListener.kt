package at.uastw.medpr.reboot.ui.fragments

/**
 * Callback interface for successful connection attempts
 */

interface OnConnectedListener {

    /**
     * Will be called on successful connection attempt
     */
    fun onConnected()
}